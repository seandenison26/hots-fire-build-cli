#!/usr/bin/env ruby


require_relative '../lib/hots-fire-build'

input = "" 
while input != "exit"
	input = gets.chomp
	hero = Hero.find_or_create_by_name(input)
	puts hero
	puts hero.name
	puts hero.title
	puts hero.role
	puts hero.franchise
end
